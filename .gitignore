# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.synctex.gz

prompt.md
/tmp

# Netlify build artifact (data/ gets copied here at deploy time)
web/data/

# Keep PDFs (they're intentional output)
# !*.pdf
