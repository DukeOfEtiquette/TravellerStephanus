# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.synctex.gz

prompt.md
/tmp
.venv

# Netlify build artifacts (data/ and references/ get copied here at deploy time)
web/data/
web/references/

# Keep PDFs (they're intentional output)
# !*.pdf
